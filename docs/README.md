# Documentation compatibility pointer

Canonical repository documentation is under [`../documentation/`](../documentation/).

This compatibility directory remains because the current Dockerfile copies `docs/` into the image. Prior documents were moved byte-for-byte into `documentation/ai/`, `documentation/human/guides/`, or `documentation/history/` and are indexed by [`documentation/tree.md`](../documentation/tree.md).
